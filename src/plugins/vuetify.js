import 'vuetify/src/stylus/app.styl'
import Vue from 'vue'
import { Vuetify, VApp, VDataTable, VDivider, VGrid, VImg, VProgressCircular, VSelect, VStepper } from 'vuetify'

Vue.use(Vuetify, {
  components: {
    VApp,
    VDataTable,
    VDivider,
    VGrid,
    VImg,
    VProgressCircular,
    VSelect,
    VStepper,
  },
})
